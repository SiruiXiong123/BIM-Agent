# OpenAI-compatible API endpoint used by chat and embedding requests.
base_url=https://your-openai-compatible-endpoint.example/v1
api_key=replace-me

# VLM/LLM used by T3-T5 and the smaller T2 classification model.
model_name=replace-me
evacuation_door_model_name=replace-me

# Must match the embedding_model value stored in every bundled manifest.json.
embedding_model_name=Pro/BAAI/bge-m3
